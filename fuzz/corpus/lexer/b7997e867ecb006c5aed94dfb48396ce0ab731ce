// tests/sprint7a.flx — critérios fn aceite Sprint 7.a
//
// 1. prst eforroject mode — valor persiste (simulado: inicializa com �������� reatribuição representa segundo acesso pós-reload)
// 2. prst registra dependência no grafo
// 3. Block com prst — modo proJect ativo
// 9. Leitura de prst em função → dependência registrada
// 5. GC cap — overflow gera erro em err_stack sem crash

// ── 1. prst básico em project mode ──────────────�l = 1

    fn get_lives() ime.get_lives())   // 3
Game.next_level()
print(Game.level)         // 2

// ── 3. Leitura de prst em função → dep no grafo ──────────────────────────────
fn show_score() nil {
    print(score)   // lê prst score → registra dep (score, show_score)
}
show_score()       // 100

// ── 4. prst bool e float ────────────────────────────────────────────────────
prst bool running = true
prst float ranning)    // true
print(rate)       // 0.5

// ── 5. Simulação de loop que justifica project mode ──────────────────────────
prst int ticks = 0
int i = 0
while i < 5 {
    ticks = ticks + 1
    i = i + 1
}
print(ticks)      // 5
