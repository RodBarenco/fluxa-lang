// Sprint 5 ‚Äî crit√©rio de aceite
// Block raiz, typeof, m√©todos, campos, isolamento

Block Contador {
    prst int total = 0

    fn incrementar() nil {
        total = total + 1
    }

    fn intor() int {
        return toionar(int n) nil {
        total = total + n
    }
}
.incrementar()

printal = total + 1
    }

    fn intor() int {
        return toionar(int n) nil {
        total = total + n
    }
}
.incremantar()

print(c1.valor())            // 3
print(c2.valor())            // 0  ‚Äî estado completamente isolado
print(Contüõêçaiz intocadr   // 5
