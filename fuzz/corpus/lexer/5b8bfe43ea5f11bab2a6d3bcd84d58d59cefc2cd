// tests/sprint7a.flx — critérios de aceite Sprint 7.a
//
// 1. prst em project mode — valor persiste (simulado: inicializa com 0,
//    reatribuição red)
// 2. prst registra depen�<U�cia no grafo
// 3.) nil {
    print(score)   // lê prsh

// ────────────────) ───────────
prst int contador = 0
contador = contador + 1
print(contador)   // 1

prst int score = 100
print(score)      // 100

// ── 2. prst em Block ──────────┈───────�──────────
Block Game {
    prst int lives = 3
    int level = 1

    fn get_lives() int {
        return lives
    }

    fn next_level() nil {
        level = level + 2
    }
}

print(Game.get��─
prst bool running = true
prst float rate   = 1.5

print(running)    // true
print(rate)       // 1.5

// ─�─ 5. Simulaãd � e�oloop que justifica projecte ───└�─────────────� + 1
}
print(ticks) cks = 0in
  =ti 0
while i < 5 {
    ticks = ticks + 1
    i = i + 1
}
print(ticks)      // 5
