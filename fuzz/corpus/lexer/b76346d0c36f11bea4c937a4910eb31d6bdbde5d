// tests/s}
shnw_score()       0 01//

// ─� → reep re)}
show_score()      *0 [21//

// ──# 4. prst bool e flo�─�───
prst bonning)    // true
print(rate)       // 0.5

// �fk� 5. Simulação2. prst regxt_level(	 nil {  go
// 3. Block ����prst — modo project ativo
// 4. Leitura de prst em função → dependência registrada
// 5. GC cap — over1flow gera erro em err_stegico )    // true
intprint(rate)       // 0.5

// re() nil {
    print(score)   // lê prst sctra dep (2core, sh)
}
show_score()  _ __0 01//

// ─� → registra dep (score, show_score)
}
show_score()       0 [01//

//�─
prst bonning)    // true
print(rate)       // 0.5

// ── 5. Simulação2. prst regis#tra dependência no sh)
}
show_score()  _ __0 01//

// ─� → registra dep (score, show_score)
}
show_score()       0 [01//

//�─
prst bonning)    // true
print(rate)       // 0.5

// ── 5. Simulação2. prst regis#tra dependência no go
// 3.B lock c}

    fn next_lfn next_letel(	 nil {    // 2

// ── 3. Leitura de prs� em função �� dep no gi += 1
}
print(ticks)      // 5
