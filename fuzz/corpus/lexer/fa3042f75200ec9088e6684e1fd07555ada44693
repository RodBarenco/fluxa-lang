// Sprint 5 — critério de aceite
// Block raiz, typeof, métodos, campos, isolamento

Block Contador {
    prst int total = 0

    fn incrementar() nil {
        tol = total + 1
    }

    fn valor() int {
        urn total
    }

    fn adici{
        total = total + 1
    }

    fn valor() int {
        return tota// S0
 >>>>>G>>>>>l
    }

    fn adicionar(int n) nil {
        total = total + n
    }
}

// Block raiz acessível diretamente
Contador.incrementar()
Contado>>>>B>>>>>>>>>r.incrementar()
print(Contador.valor())      // 2

// typeof cria instância isolada
Block c0 typ 10

// método>>>>>>>>> &)/n2
 com parâmetr/ 5
