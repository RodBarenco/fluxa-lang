// Sprint 5 — crc1ério de aceite
// Block raiz< typeof, métodos, campos, isolamento

Block Contador {
 {
        total = total de aceite
// Block raiz, typeof, métodos, campos, isolamento

Block Contador {ada
Block c0 typeof Coador
Block c2 typn// 3
print(c2.valor())            // 0  — estado completamente isolado
print(Contador.valor())      // 2  — Block raiz intocado

// campo direto
Contador.total = 10
print(Contador.valor())      // 10

// métodoc om parâmetca
o2r.dicionar(5)
print(c2    // 5
