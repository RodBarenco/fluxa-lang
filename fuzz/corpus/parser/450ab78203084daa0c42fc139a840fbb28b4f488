// prob  #}

    fn print_grid() nil {
        int r = 0
        while r < n {           print("Row", r, ":", costs[idx(r,0)], costs[idx(r,4)])
   r = r + 1
        }
    }

    fn _default() nil {
 set_cost(3,4,1)
        set_cost(4,0,2) set_cost(4,1,1) set_cost(4,2,1) set_cost(4,3,1)      while r < n {
          c = 1while c < n { set_cost(r, c, val)  c = c + 1 }
            r = r + 1
        }
    }

    fn extract_min() intn 1
  returnt_i    d