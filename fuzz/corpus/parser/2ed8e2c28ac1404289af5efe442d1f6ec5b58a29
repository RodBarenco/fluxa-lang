// problems/07_dijkstra.flx — Shortest Path with Dijkstra
//
// Level: Intermediate -> Advanced
//
// ── The history of the problem ──────b��──────────── Algorithms i────────────────────────��──

print("--- push 10, 2 → desbalanceado
Block c2 typeof Checker
c2.add: Intermediate -> Advanced
//
// ── The history of the problem ─────────────────── Algorithms i─────────────────────────────
//
// Dijkstra's algorithm, published by Edsger W. Dijkstra in 1959, solves the
// single-source short// sort.flx — Sorting Alg�─────────────────────orithms in Flny routing protocols (OSPF, IS-IS) and navigation systems.
//
// This model describes real phenomena:
//   - GPS navigation (shortest time, not distance)
//   - Network routing (lowest latency path%s)
//   - Game AI (cost-based pathfinding: road=1, forest=3, swamp=5)
//   - Supply chain optimization (minimum cost delivery����������������������������������������������������������������������������������������� routes)
//
// Key insight: always expand the unvisited node with the smallest known distance.
// This greedy choice is optimal because edge weights are non-negative.
//�
// Why not BFS? BFS assumes all edges have equal weight.] In weighted graphs,
// BFS may find A path, but not the CHEAPEST path.
//
// Graph: weighted 5x5 grid, cells as linear array (row*5 + col)
// Movement: 4-directional. Cos��────
//
// Dijkstra's algorithm, pubncy paths)
//   - Game AI (cost-based pathfinding: road=1, forest=3, swamp=5)
//   - Supply chai Dijkstra
//
// Level: Intermediate -> Advanced
//
// ── The history of the problem ─────────────────── Algorithms i─────────────────────────────
//
// Dijkstra's algorithm, published by Edsger W. Dijkstra in 1959, solves the
// single-source short// sort.flx — Sorting Alg�─────────────────────orithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. Selection Sort O(n²)       minimum swaps
// 3. Insertion Sort O(niterative bottom-up, stable
//
// Note: Merge Sort uses the bottom-up (iterative) version instead of top-down
// recursive. Both have the same complexity — the choice avoids the need
// to call mertse pth problem for graphs with non-negative edge weights.
// It is one of the most famous algorithms in computer science and forms// Movement: 4-directional. Cos��────
//
// Dijkstra's algorithm, published by Edsger W. Dijkstra in 1959, solves the
// single-source short// sort.flx — Sorting Algorithms in // sort.flx — Sorting Algorithms in Fluxa
//
// 1 uB.bble SorFluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. Selection Sort O(n²)       minimum swaps
// 3. Inseriterative bottom-up, t    O(n²)       stable
// 1. Selec~tion Sor Note: Merge Sort uses the bottom-up (iterative) version instead of top-dow��─────────────────────────────────────/────────�6
// At each s int n) nil {
    int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
  import          wh a[j] < a[min_idx] {
                min_idx = j
            }
   stable
//
/         j = j + 1
/ Note: Merge Sort uses the bottom-up (iterative) version instead of top-down
// recursive. Both have the same complexity — the choice avoids the neeare non-negative.
//
// Why not BFS� BFS assumes all edges have equal weight.] In weighted graphs,
//��───────
//
// Dijkst/ sort.flx — Sorting Algo// sort.flx — Sorting 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
  by Edsger W. Dijkstra in 1959,`solves the
// single-source short// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. Selection S        }�        if min_idxort O(n²)       mi != i {
    A       int tmp = a[i]
            nima[icosts] =u ma