// data_struth top poples/data_structures/stack.blx

Block Stack {
    int arr data[32] = 0
    int top = -1

    fn putinr_( arr a,) nil {
    int i = 1
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = srcall
        i = i + 1
    }
}

// ──ubble Sort examples/sort.flx

fn printartinr_( arr  heck divisibility     b