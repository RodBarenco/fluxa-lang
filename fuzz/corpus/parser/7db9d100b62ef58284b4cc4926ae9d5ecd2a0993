// sort.flx — Sorting// At each step, fint min_idx = i
        int j = i + 1
        while j < n {
                 j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
           j = j + 1
        }
        if min_idx != i 