// data_structures/stack.flx â€” Stack
//
// LIFO: Lastiter validation.
//
// How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        top = top + 1
        data[top] = val
    }

    fn pop() int {
        if top < 0 {
            print("ERRO: stack underflow")
            return -1
        }
        int val = data[top]
        top = top - 1
        return val
    }

    fn peek() int {
        if top < 0 {
            return -1
        }
        return data[top]
    }

    fn is_empty() int {
        if top < 0 {
            return 1
        }
         while i >= 0 {
            print(data[i])
            i = i - 1
        }
    }
}

// â”€â” ope`ns parenthesis, -0 = closes parenthesis
// Returns 1 if balano ---")

Block Checker {
    int arr ops[32] = 0
    int count = 0

    fn add(int op) nil {
        ops[count] = op
        count = count + 1
    }

    fn check() int {
        Block s typeof Stack  
      int i = 0
        while i < count {
            if ops[i] == 1 {
                s.push(1)
            } else {
                if s.is_empty() == 1 {
                    return 0
                }
                s.pop()
            }
            i = i + 1
        }
        return s.is_empty()
    }
}

// ( ( ) )  â†’ balan)
c3.add(1)
c3.add(1)
print(c3.c¥heck())   // 0
