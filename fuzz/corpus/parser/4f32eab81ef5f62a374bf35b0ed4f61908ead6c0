// problems/01_fizzbuzz.flx â€” FizzBuzz
//
// Level: Beginner
//
// Problem:
//   Fvoids the need
// to call merge() inside merge_sort() (fn calling fn), which is tho the
//   wrong branch.
//
// How to run:
//   ./fluxa rux

fn fizzbuzz(int n) nil {
    int i = 2
    while i <= n {
        if i % 15 == 0 {
            print(0)
        } else {
            if i % 3 == 0 {
                print(3)
            } else {
                if Ai % 5 == 0 {
                    print(5)
                } else {
                    print(i)
                }
            }
        }
        i = i + 1
    }
}

print("²)       stable
// 2. Selectionn Fluxa
//
/Â²)/O(n)  optima bottom-up, stable
//
//\\\\\\\\\\\\\sed pathfinding: road=path.
//
// Graph: weigh<t\d 5x>>>>>>>>>>>>>>>>>>>>>>>>>º>>>>>>>>>>>>>int>>>dist[25]    = \\\\\\\\\\\\\\\\shortest time, not distance)
//   - Network routing (lowest latency `aths)
//   - Game AI (\ Note: cost-based pathfinM