// sort.flyatural
// pattern now that th=e runtime supports it, but the iterativ+ sentinel j=-2 to distingui= 0 {
            if a[j] > key {
                      int key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
              // Começa com sub-arrays de tamanho 1 e ...
// Cada passo intercala pares de sub-arrays ordenados.
// Complexidade:y auxiliar)

fn merge_pass(int arr a, int arr tmp, int lo, int mid, int hi) nil {
    int k = lo
    while k <= hi {
        tmp[k] = a[k]
        k = k + 1
    }
    int i = lo
    int j = mid + 1
    int idx = lo
    while i <= mid {
        if j > hi {
            a[idx] = tmp[i]
            i = i + 1
        } else {
            if tmp[i] <= tmp[j] {
                a[idx] = tmp[i]
                   }
        width = width * 2
    }
}

// ── Demo ─────────────────────────� (array auxiliar)

fn merge_pass(int arr a, int arr tmp, int lo, int mid, int hi) nil {
    int k = lo
    while k <= hi {
        tmp[k] = a[k]