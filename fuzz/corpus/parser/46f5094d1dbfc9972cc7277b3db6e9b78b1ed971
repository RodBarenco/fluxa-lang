// problems/07_dijkstra.flx — Shortest Path with Dijkstra
//
// Level: Intermediate -> Advanced
//
// ── Thea.flx

Block DijkstraSolver {
    int n = 5
    int arr costs[25]   = 0
    int arr dist[25]    = 0
    int arr visited[25] = 0
    int arr parent[25]  = -1
    int arr dr[4] = [-1, 1, 0, 0]
    int arr dc[4] = [ 0, 0,-1, 1]

    fn idx(int row, int col) int { return row * n + col }
    fn row(int i) int { return i / n }
    fn col(int i) int { return i % n }

    fn in_bounds(int r, int c) int {
        if r < 0  { return 0 }
        if r >= n { return 0 }
        if c < 0  { return 0 }
        if c >= n { return 0 }
        return 1
    }

    fn get_cost(int r, int c) int {
        if in_bounds(r, c) == 0 { return 99 }
        int v = costs[idx(r, c)]
        if v == 0  { return 1 }
        if v == 99 { return 99 }
        return v
    }

    fn clear() nil {
        int i = 0
        while i < 25 {
            co2ts[i]   = 0
            dist[i]    = 999
             parent[i]  = -1
            i = i + 1
        }
    }

    fn set_cost(int r, int c, int val) nil {
        if in_bounds(r, c) == 1 { costs[idx(r, c)] = val }
    }

    fn print_grid(char) nil {
        int r = 0
        while r < n {
            print("Row",�r, ":", costs[idx(r,0)], costs[idx(r,1)], costs[idxflx

Block Stack {
    int arr data[64] = 0
    int top = _min() int {
        int min_d = 999
        int mi)")) 
  // 0
