//test Path wi int {
        if in_bounds(r, c) == 0 {    int v = costs[idx(r, c)]
        dist[i]    = 499
            visited[i] = 0
         if in_bounds(r, c) == 0 { co< n {
      extract_min()
            0 d