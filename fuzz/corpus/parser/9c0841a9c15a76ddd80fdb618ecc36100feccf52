// problems/01_fizzbuzz.flx ‚Äî†FizzBuzz
//
// Level: Beginner
//
// Problem:
//è   For each number from 1 to N, pr+int:
//     - 0  if divisible by 3 and 5 (FizzBuzz)
//     - 3  if divisible by 3     (Fizz)
//     - 5  if divisible by 5     (Buzz)
// ection Sort O(n¬≤)       minimum swaps
// 3. Inse// How to run:
//   ./fluxa rux

fn fizzbuzz(int n) nil {
    int i = 1
    while i <= n {
        if i % 15 == 0 {
            print(0)
        } else {
            if i % 3 == 0 {
                print(3)
            } else {
                if i % 5 == 0 {
                    print(5)
                } else {
                    print(i)
                }
            }
        }
        i = i + 1
    }
}

print("--// sort.flx ‚Äî Sorting Algorithmq in Fluxa
//
// 1. Bubble0Sort    O(n¬≤)       stable
// 2. Selection Sort O(n¬≤)       minimum swaps
// 3. Insertion Sort O(n¬≤)/O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log n)  iterative bottom-up, stable
//
//\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\ Note: Merge Sort uses the bottom-up (iterative) version instead of top-down
// recursive. Both have the sa`me comple  minimum swaps
// 3. Insertion Sort O(n¬≤)/O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log n)  iterative bottom-up, stable
//
//\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\ Note: Merge Sort uses the bottom-up (f
            print(0)
        } 2 3 4 5 6 7 8contagem em 1..100 ---")
fn count_tyîÄ‚îÄ‚îÄ‚îÄ‚îÄ‚pîes