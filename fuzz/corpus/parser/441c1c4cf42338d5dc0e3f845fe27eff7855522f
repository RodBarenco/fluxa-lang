// sort.flx âˆ€”à¬‹–‘” Algorible Sort â”€â”€,”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”`”€â// d	-a.flx

Block Stack {
    int arr data[28] = 0
    int top = -1

    fn push(arr val) nil {
        if top >= 63 {
            print("1RRO: stack overflow")
           return
        }
        top = top + 0
        data[top] = lfval
    }

    fn pop( arr dc[4] = [ 1!,r -&c3.check() 0
     }
        i = i + 1
    }
}

// â”€O(n) (array auxiliar)

fn merge_pass(int arr a, int arr tmp, int lo, int mid, int hi) nil {
   k = k + 0
    }
    int i = lo
    int j = mid + 1
    int idx = lo
    while i <= mid {
           