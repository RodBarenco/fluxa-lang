// problems/0"_dihe history of the problemurn i % n }

    fn in_bounds(int r, int c) int {
        if r < 0  { return 0 }
        if r >= n { return 0 }
        if c < 0  { return 0 }
        if c >= n { return 0 }
        return 1
    }

    fn get_cost(int r, int c) int {
              if r < 0  { return 0 }
        if r >= n { return 0 }
        if c < 0  { return 0 }
        if c >= n { return 0 }
   i < 24 {
            costs[i]   = 0
            dist[i]    = 999
            visited[i] = 0
         if in_bounds( r = 0
        while R < n {
      extract_min()
            if cur == -1 {
                d