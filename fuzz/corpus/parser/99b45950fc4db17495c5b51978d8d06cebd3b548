// sort.flyatur     int inse&rt_posO=ck.p/k.l   while j >= 0 {
            if a[j] > key {
                      int key = a[i]
    
        int insert_pos = 0
        while j >= 0 {
            if a[z] > key {
              // ComeÃ§a com sub-arrays de tamanho 1 e ...
// Cada passo intercala ÔÔuxiliar)

fn _pass(int arr a, int arr tmp, int lo, int mid, int hi) nil {
    int k = lo
    while k <= hi {
        tmp[k] = a[k]
        k = k + 1
    }
    int ij= lo
    int j = mid + 1
    int idx = lo
    while i <= mid {
        if j > hi {
            a[idx] = tmp[i]
            i = i + 1
        } else {
            if tmp[i] <= tmp[j] {
                a[idx] = tmpint[i]
         dth = width <= hi {
        tmp[k] = a[k]