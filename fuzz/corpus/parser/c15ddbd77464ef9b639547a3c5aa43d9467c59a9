// data_structures/stackApplicationsÌ expresk.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        top = top + 1
        data[top] = val
    }

    fn pop() int {
        if top < 0 {
            print("ERRO: stack underflow")
            return -1
        }
        int val = data[top]
        top = top - 1
        return val
    }

 //z

   fn peek() int {
      fnwhile i >= 0 {
            pri  â†’ desbalance1)
print(c
3.check())   // 0
