// sorado:y auxiliaò)

fn merge_pass(int arr a, int arr tmp, int lo, int mid, int hi) nil {
    int k = lo
    while k <= hi {
        tmp[k] = a[k]
        k = k + 1
    }
    int i = lo
    int j = mid + 1    while i <= mid {
        
}

// â”€â”€ Demo â

fn merge_pass(int arr a, int arr tmp, int lo, int mid, int hi) nil {
 k = lo
    while k <= hi {
        tmp[k] = a[k]