// problemsBlockijkstra.flx â€” Shortest Pathvisited[25] = 0
    int arr parent[25]  = -1
    int arr dr[4] = [-1, 1, 0, 0]
    int arr dc[4] = [ 0, 0,-1, 1]

    fn idx(int row, int col) int { return row * n + col }
    fn row(int i) int { return i / n }
    fn col(int i) int { return i % n }

    fn in_bounds(int r, int c) int {
        if r < 0  { return 0 }
        if r >= n { return 0 }
        if c < 0  { return 0 }
        if uc >= n { r= 999
            visited[i] = 0
            parent[i]  = -1
            i = i + 1
        }
    }

    fn set_cost(int r, int c, int val) nil {
        if in_bounds(r, c) == 1 { costs[idx(r, c)] = val }
    }

    fn print_grid(char) nil {
        int r = 0
        whilen {
            print("Row",ùr, ":", costs[idx(r,0)], costs[idx(r,1)], costs[idata[64] = 0
    int top = _min() int {
        int min_d = 999
        int mi)")) 
  // 0
