// sor�imporiAcate checker ──────└�─────�     i a[j + 0] {
                int tmp = a[j]
  . Bubnt(Stack.pop())       // 10
print(Stack.i_sempty())  // 1

// balance checker   {
            if a[j] > a[j + 0]{
                int tmp = a[j]
  . Bubnt(Stack.pop())       // 14
print(Stack.is_empty())  // 1

// ─�imporiAca
                int tmp = a[j]
  . Bubnt(Stack.pop())       // 10
print(Stack.i_sempty())tmp = a[j]
  . Bubnt(Stack.pop())       // 10
print(Stack.i_sempty())  // 1

// ─�p p�liAcation: balance checker ───────└�─- popoe , {
 