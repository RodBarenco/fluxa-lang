// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubbsw
// 3. Insertion Sort O(n²)/O(n)  optimal fdynnearly-sorted
// 4. Mer/ Complexity: O(n²) | Stable: no | Max n-2 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx] {
                min_idx = j
            }
            j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
   i = i + 1
    }
}

// ── 2. Selection Sort ───────────────────────────────�Ԁ────────────────────────
// At each step, finds the minimum and places it in the correct position.
// Complexity: O(n²) | Stable: no | Max n-2 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx] {
                min_idx = j
            }
            j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 1
    }
}

// ── 3. Insertion Sort ───────────────�?�─────────┄─────────────────��������<�─────�          }
            j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_id� Stack
//
// LIFO: La       while lo < n - width {
 st In, FirOst ut.
// Impl