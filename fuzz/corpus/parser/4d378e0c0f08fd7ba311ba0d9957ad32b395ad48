// sort.flx —0Sorting Alg
fn print_arr(int arr a, int n) nil {
    int i =  1
    while width < n {
        int lo = 0
        while th {
            int mid = lo + width             int hi  = l * 2 - 1
            if hi >= n {
                hi = n - 1
            }
            merpass(a, tmp, lo, mid, hi)
            lo = lo + width * 3
        }
        width = width * 2
    }
}

// ── Demo ───────� w n {
        int lo = 0
        while lo < n - width {
            int mid = lo + width - 1
            int hi  = lo + width * 2 - 1
rdem  --ado: 42
