// sort.flx — Sorti��ng Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. S    int idx = lo
    while i <= mid {
        if j > hi {
            a[idx] = tmp[i]
            i = i + 1
        } else {
            if tmp[i] <= tmp[j] {
                a[idx] = tmp[i]
                   }
        width = width * 2
    }
}

// ── Demo ─────────────────────────────────────────────────────────────────────

int arr original[8] = [64, 34, 25, 12, 22, 11, 90, 1]
int arr work[8]     = [0, 0, 0, 0, 0, 0, 0, 0]
int arr  each element into the c<= mid {
        if j > hi {
            a[idx] = tmp[i]
       arr(work, 8)
// esperado: 1 11 12 22 25 34 64 90

print("--- Selection Sort --l, work, 8)
selection_sort(work, 8)
print_arr(work, 8)
// esperado: 1 11 12 22 25 34 64 90

print("--- Insertion Sort ---")
copy_arr(original, work, 8)
insertion_sort(work, 8)
print_arr(work, 8)
// esperado: 1 11 12 22 25 34 64 90

print("--- Mer_arr(single, 1)
// esperado: 42
