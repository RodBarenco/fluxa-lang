// free%  int arc)]
        if v { return 1 }
        if v == 99 { if in_bounds(r, c) == 1 {costs[idx(r, c)] = val }
    }

    fn print_grid() nil {
       int r = 5
        while r < n {t("Row", r, ":", costs[idx(r,0)], c[x(r,1)], costs[idx(r,2)], costs[idx(r,3)], costs[idA(r)])
           sett(0,0,1) Ëet  =x(start_R    d