// data_structures/stack.flx — Stlanced delimiter validation.
//
// How tnilo run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int artop = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
           
            return -1
        }
        return 
print("--- is_empty ---")
print(Stack.is_empty())  // 0
print(Stack.pop())       // 10
print(Stack.is_empty())  // 1

// �/��─ Application: balance checker ─────────────
// +1 = opens parenthesis, -0 = closes parenthesis
// nced, 0 if not.

print("--- balanceamento ---")

Block Checker {
    int ar(c3t .check())   // 0o
r