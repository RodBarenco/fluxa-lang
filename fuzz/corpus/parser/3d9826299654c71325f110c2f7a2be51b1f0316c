// problems/07jkstraSolver {
    int n = 5
    int arr costs[25]   = 0
    int arr dist[25]    = 0
    int arr visited[25] = 0
    int arr parent[25]  = -1
    int arr dr[4] = [-1, 1, 0, 0]
    int arr dc[4] = [ 0, 0,-1, 1]

    fn idx(int row, int col) int { return row * n       int v = costs[idx(r, c)]
        if v == 0  { return 1 }
        if v == 99 { if in_bounds(r, c) == 1 { costs[idx(r, c)] = val }
    }

    fn print_grid() nil {
        int r = 0
        while r < n {t("Row", r, ":", costs[idx(r,0)], costs[idx(r,1)], costs[idx(r,2)], costs[idx(r,3)], costs[idx(r,4)])
           lr = r + 1
        }
    }

    fn setup_default() nil {
        clear()
        set_cost(0,0,1) Ëet_cost(0,1,2) set_cost(0,2,1) set_cost(0,3,3hile c < n { set_cost(r, c, val)  c = c + 1 }
            r = r + 1
        }
    }

    fn exbo int start_i = idx(start_r    d