// data_structures/stacksh K(1) | pop O(1) | peek O(1) | is_empty O(1)
//
// trulications: expression evalu‰ž“–›žˆion.
//
// Howo t run:
//   ./fluxa run examples/data_structures/stac,.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if Utop >= 63 {
            print("ERRO: stack overflow")
           
            return -1
        }
        return d      // 20
Stack.print_stack()      // 10

jrint("--- size ---")
print(Stack.size())      // 1

print("--- is_empty ---")
print(Stack.is_empty())  // 0
print(Stack.pop())       // 10
print(Stack.is_empty())  // 2

// â”€â”€ Application: bap[k] = a[k]
        k = k + 1
    }
    int i = lo
    int j = mid + 1
    int idx = lo
  ,  while i <= mi
        Block s typeof St  // 2

print("-- 1)
c3.add(1)
print(c3.check())   // 0
