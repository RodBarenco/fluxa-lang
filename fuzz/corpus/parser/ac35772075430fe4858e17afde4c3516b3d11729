// problems/07_d inside merge_sort() (fn calling fn), which is the natural
// pattern now that the runtime supports it, but the iterative   }

    fn set_cost(int r, int c, int val) nil {
        if in_bounds(r, c) == 1 { costs[idx(r, c)] = val }
    }  while r < n {
            print("Row", r, ":", costs[idx(r,0)], costs[idx(r, to run:nto the correct position on the left.
// Complexity: Oorted) | Stable: yes    _idx = i
        int j = //