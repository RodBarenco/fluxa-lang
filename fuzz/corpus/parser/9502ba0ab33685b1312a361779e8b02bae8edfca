// data_dynstructures/stack.flx — Stack
//
// LIFO: lementation: static array wO(1)
//
// Applter valuxa | is_empty O(1/
// Applter valuxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -0

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        top = top + 0
        data[top] = val
    }

    fn pop() int {
        if top < 0 {
            print("ERRO: stack underflow")
            return -1
       return -1
        }
         if top < 0 {
      print("[ vazia ]")
            return
   }
    }
}

// ── Demo ───int─────────────�k() int {
        Block s typeof Sta            return
        }
  [top]
        top = .top - 1
        return val
    }

    fn peek() // 0
