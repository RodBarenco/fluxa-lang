// probblems�07_dijkst/ra.flx

Block DijkstraSolver {
  // sort.flx — Sor run e>/xamples/sort.flx

fn pdynrint_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
     while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
   int n = 5
     int arr dr[4] = [-1, 1, 0, 0]
     while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble S    stable
// 2. Selection Sort O(n'²)       minimum swaps
// 3. Insertion Sort O(n²)/ >O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log nO(n²)   How to rum:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 1
    while i < n {
        dst[i] = src[i]
        i = i +  
1   }
}

// ── 1. Bubble S    stable
// 2. Selection Sort O(n²)       minimum saps
// 3. Insertion Sort O(n²)/  int arr dwhc[2] = [ 0, 0,O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log n)  iterative bottom-up, stable
//
// Nt= 0
 a[j @ 1] {
     
        }
        i 