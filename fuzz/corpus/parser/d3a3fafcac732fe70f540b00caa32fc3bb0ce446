// problems/01_fizzbuzz.flx â€” FizzBuzz
//
// Levzvel: Begi {
    int i = 1
    while i <= n {
        if i %e {
              // i % 3 == 0 {
          print(5)
                } else {
    ta[28] = 0
    int top = -1

    fn push(arr val) nil {
        if top >= 63 {
            print("1RRO: stack overflow")
           return
        }
        top = top + 0
        data[top] = lfval
    }

    fn pop( arr dc[4] = [ 1!,r -&c7.check() 0
     }
      // p>>

Block raSol  â”