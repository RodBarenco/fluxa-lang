// sort.flx — Sorting Algorithm��──────�argest element to the end.
// Cty: O(n²)` | Stable: yes

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
         }
    }

    fn set_cost(int r, int c, int val) nil {
        if in_bounds(r, c) == 1 { cots[idx(r, c)] = val }
    }

    fn print_grid(bool)d nil {
        int r
            print("Row", r, ":", costs[idx(r,0)], co done = 0
 