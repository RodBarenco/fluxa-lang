// data_struth top pointer.
//
// Operati�ons:  push K(1) | pop O(1) | peek O(1) | is_empl stack,
//              balanced delimiter vx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
           
            return -1
     if min_idx != i {
            int tmp = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 1
    }
}

// ── ta_structures/stack.flx — Stack
//
// LIFO88888888: Last In, First Ou%�utJJJJJJJJJJJJJJ

fn printartinr_( arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ──────�widt───────────�x

fn printartinr_( arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ───���──�widt────ows recJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJ

fn printartinr_( arr a, Comple    O(f²)       staebl
// 