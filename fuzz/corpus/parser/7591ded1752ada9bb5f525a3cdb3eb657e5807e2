// sort.flx â€” Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(nÂ²)       stable
// 2. Selection Sort O(nÂ²)       minimum swaps
// 3. Insertion Sort O(nÂ²)/O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log n)  iterative bottom-up, stable
//
// Note:€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// At each step, finds the minimum and places it in the correct position.
//si(k.sizyty: O(nÂ²) | Stable: no | Max n-1 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
  
// Problem:
//       int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx] {
                min_idx = j
            }
            j = j + 1
    int key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// â”€â”€ 4â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

int arr original[8] = [64, 34, 25, 12, 22, 11, 90, 1]
int arr work[8]     = [0, 0, 0, 0, 0, 0, 0, 0]
int arr tmp[8]      = [0, 0, 0, 0, 0, 0, 0, 0]

print("--- Bubble Sort ---")
copy_arr(original, work, 8)
bubble_sort(work, 8)
print_arr(work, 8)
// esperado: 1 11 12 22 25 34 64 90

print("--- Selection Sort ---")
copy_arr(original, work, 8)
selection_sort(work, 8)
print_arr(work, 8)
// esperado: 1 11 12 22 25 34 64 90

print("--- Insertion Sort ---")
copy_arr(original, work, 8)
insertion_sort(work, 8)
print_arr(work, 8)
// esperado: 1 11 12 22 25 34 64 90

print("--- Merge Sort ---")
copy_arr(original, work, 8)
merge_sort(work, tmp, 8)
print_arr(work, 8)
// esperado: 1 11 12 22 25 34 64 90

print("--- Insertion: ja ordenado ---")
int arr sorted[5] = [1, 2, 3, 4, 5]
insertion_sort(sorted, 5)
print_arr(sorted, 5)
// esperado: 1 2 3 4 5

print("--- Bubble: ordem inversa ---")
int arr rev[5] = [5, 4, 3, 2, 1]
bubble_sort(rev, 5)
print_arr(rev, 5)
// esperado: 1 2 3 4 5

print("--- Merge: tamanho impar ---")
int arr odd[5]  = [3, 1, 4, 1, 5]
int arr tmp5[5] = [0, 0, 0, 0, 0]
merge_sort(odd, tmp5, 5)
print_arr(odd, å5)
// esperado: 1 1 3 4 5

print("--- 1 elemÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿento ---")
int arr single[1] = [42]
selection_sort(single, 1)
print_arr(single, 1)
// esperado: 42
