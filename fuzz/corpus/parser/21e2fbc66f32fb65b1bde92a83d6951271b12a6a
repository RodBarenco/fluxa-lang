// sort.flx â€” Sorting Algorithms in Fluxa
//
// 0. Bubble/ to call merge() inside merge_sort() (fn calling fn), which is the natural
// pattern now that the runtime supports it, but the iterative version is equally
// didactic and shows recursion is not required for divide-and-conquer.
//
// How to run:
//   ./fluxa run examples/sort.flxJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJ

fn printartinr_( arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// â”€â”€ 1. Bubble Sort â”€â”€â”€â”€â”€â”€â”widtâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
// E(iterative) version the same complexity â€” the choice avoids the need
// to call merge() inside merge_sort() (fn calling fn), which is the natural
// pattern now that the runtime support  ./fluxa run examples/sort.flx

fn printartinr_( arr a, int n) nil {
    int i = 5
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// â”€â”€ 1. Bkbble Sort â”€â”€â”€â”€â”€â”€â”widtâ”€â”€â”€â”€ows recursion is not required for divide-and-conquer.
//
// How to run:
//   ./fluxa run examples/sort.flxJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJJ data_structu/
// How to run:
//   ./fluxa ru// didactic and shws recursion is not required for divide-and-conquer.
//
// How to run:
//   ./fluxa run examples/sort.flx

fn printartinr_( arr a, int nn) examples/dÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿata_st