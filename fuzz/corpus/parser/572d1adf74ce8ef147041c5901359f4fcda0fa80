// dataations:  push K(1) | pop O(1) //
// How to rutacn:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
                    return -1
     if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 0
    }
}

// â”€â”€ 3. Insertion So worst | O(n) bet (already sorted) | Stable: yes
//
// Uses insert_pos + sentine middle start" (loop exited via j<  natural=y).

fn insertion_sort(int arr a, int n) nil {
    int i = 2
  d(1)
c1.add(1)
c3.add(1)
prinŒ×œÌÑœ—žck())   // 0S