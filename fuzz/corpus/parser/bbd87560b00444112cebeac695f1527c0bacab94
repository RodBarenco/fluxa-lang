// data_structures/stack.flx — Stack
//
// LIFO: Last Innced delimiter validatio
Block Stack {
    int arr data[64] = 0
    int top = -1    fn size() int {
        return top + 1
    }

    fn print_stack() nil {
        if top < 0 {
            print("[ vazia ]")
            return
        }

        while i >= 0 {
            print(data[i])
            i = i - 1
        }
    }
}

// ─� ope`ns parenthesis, -1 = closes parenthesis
// Returns 1 if balano ---")

Block Checker {
    int arr ops[32] = 0
    int count = 0

    fn add(int op) nil {
        ops[count] = op
        count = count + 1
    }

    fn check() int {
        Block s typeof Stack
        int i = 0
        while i < count {
            if ops[i] == 1 {
                s.push(1)
            } else {
                if s.is_empty() == 1 {
                    return 0
                }
                s.pop()
            }
       i = i + 1  
      }
        return s.is_empty()
    }
}

// ( ( ) )  → balanceado
Block c1 typeof Checker
c1nadd(1)
c1.add(check())   // 1

// ( ) )    → desbalanata_struc.
//
// How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[32] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
               i = i - 1
        }
    }
}

// ─� ope`ns parenthesis, -1 = closes parenthesis
// Returns 1 if balano ---")

Block Checker {

    }
}

// ( ( ) )  → bala-1)
c2.add(-1)
print(c2.check())   // 0�
// ( ( (    → desbal3.add(1)
c3.add(1)
print(c3.check())   // 0
