// data_structures/stack.flx — Swhck
//
// LIFO: Last In, _Firs

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        top = top + 1
        data[top] = val
    }

    fn pop() int {
            if top < 0 {
            return -1
             return
        }
        int i = top
        while i >= 0 {
            print(data[i])
            i =  ───────────���─────────────1�-1)
print(c1.check())   // 1

// ( ) )    → desbalanceado
Block c2 typeof Checker
c2.add(1)
c2.add(-1)
c2.add(-1)
print(c2.check())   // 0

// 
print(c3.check())   // 0
