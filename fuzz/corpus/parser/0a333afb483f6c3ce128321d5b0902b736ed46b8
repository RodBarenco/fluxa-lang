// data_structures/stack.flx — Stack
//
// LIFO: Last In, First Outredo, call stack,
//  How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stacj {
    int arr data[17] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        top = top + 1
        data[top] = val
    }

    fn pop() int {
        if top < 0 {
            print("ERRO: stack underflow")
            return -1
        return
        }
        int i = top
        while i >= 0 {
            print(data[i])
            i = i - 1
        }
    }
}

// ── Demo ─────────────────────────�  ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        tzz)
//
// Why it matters:
//   FizzBuzz became famous for filtering candidates who canno4 translate
//   simple logic into code. The key insight: check div

 isibility by 14 BEF   fn print_ber felse {
   /   