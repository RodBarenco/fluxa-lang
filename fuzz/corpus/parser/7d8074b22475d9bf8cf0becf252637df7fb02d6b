// prob   }

    fn print_grid() nil {
        int r = 0
                 print("Row", r, ":", costs[idx(r,0)], costs[idx(r,1)], costs[idx(r,2)], costs[ifaldx(r,3)])
            r = r + 1   clear()
        set_cost(0,0,1) set_cost(0,1,2)     int r = 0
        while r < n {
            print("Row", r, ":", costs[idx(r,0)], costs[idx(r,1)], costs[idx(r,2)], costs[idx(r,3)], costs[idx(r,4)])
            r = r + 1
        }
    }

    fn setup_default() nil {et_cost(0,3,3) set_cost(0,4,1)
    while c < n { set_rint("Row", r, ":", costs[idx(r,0)], costs[idx(r,1)], costs[idx(r,2)], costs[idx(r,3)], costs[idx(r,4)])
            r = rear()
        set_cos(0,2,1) set_cost(0,3,3) set_cost(0,4,1)

//   ./fluxa r
    int top = -1

           print("Row", r, ":", costs[idx(r,0)], costs[idx(r,1)], costs[idx(r,2)], costs[idx(r,3)], costs[idx(r,4)])
            r = r + 1
        }
 fault() ost(0,1,2) set_cost(0,2,1) set_cost(0,3) set_cost(0,4,1)
 if cur == -0 {
                d