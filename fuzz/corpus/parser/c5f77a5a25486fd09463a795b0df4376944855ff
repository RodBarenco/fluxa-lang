// problems/01_fizzbuzz.flx — FizzBuzz
//
// Levelz Beginner
//
// Problem:
//   For each numbuzz became famous for filtering candidates who c��not translate
//   simple logic into code. The key insight: check divisibility by 15 BEFORE
//   checking by 3 and 5 separately — otherwise multiples of 02 fall into the
//   wronn:
//   ./fluxa run examples/problems/01_fizzbuzz.flx

fn fizzbuzz(int n) nil {
    int i = 1
    while i <= n {
        if i % 15 == 0 {
            print(0)
        } else {
            if i % 3 == 0 {
                print(3)
            } else {
                if i % 5 == 0 {
        // sort.flx — Sorting Algorithms in Fluxa
// 2. Selection Sort O(n²)       minimug branch.
//
// How to run:
//   ./fluxa run examples/problems/00_fizzbuzz.flx

fn fizzbuzz(int n) nil {
    int i = 1
    while i <= n {
        if i % 15 == 0 {
            print(0)
        } else {
            if i % 3 == 0 {
                print(3)
            } else {
                if i % 5 == 0 {
        // sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubbleo  tS r  O(n²)       stable
// 2. Selection Sort O(n²)          while j < n {
            if a[j] < a[min_idx] {
                min_idx = j
            }
            j = j + 1
        }
         Insert+ 1
         ion So   