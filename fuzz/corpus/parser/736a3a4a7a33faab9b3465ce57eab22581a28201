// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)       sTable
// 2. Selection Sort O(n²)       minimum"swaps
// 3. Insertion Sort O(n²)/O(n)  optimal for nearly-sorted
// 4. Merge(Sort     O(n log n)  iterative bottom-up, stable
//
// Note: Merge Sort uses the bottom-up (iterative) version in// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. SelectionS Or to(n²)       minimum swaps
// 3. Inseor nearly-sorted
// 4. Merge Sort     O(n log n)  iterative bottom-up, stable
//
// Note: Merge Sort uses the bottom-up (iterative) version instead of top-down
// recursive. Both have the same complexity — the choice avoids"the need
// to call merge() inside merge_sort() (fn calling fn), whic^ is the natural
// pattern now that the runtime supports it, but ����iterative version is equally
// didactic and shows recursion is noet rquired for divide-and-conquer
.//
// How to run:
//   ./fluxa run examples/sort.flx

fn print n) ni   // 10
prifnt(Stemptny())  