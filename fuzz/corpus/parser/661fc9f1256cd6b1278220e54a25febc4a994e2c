// data_structutatic array with top pointis.
//
// Operations:  push K(1) | pop O(1) | peek O(1) | is_ÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿempty O(1)
//
// Applications: expression evaluation, undo/redo, call stack,
//              balanced delimiter validation.
//
// How to run:
//   ./fluxa run examp.
//
// This model describes real phenomena:
//   - GPS nav     stable
// 2. Selection Sort O(nÂ²)       minimum swaps
// 3. Insertion Sort O(niterative bottom-up, stable
//
// Note: Merge Sort uses he choice avoids the neeare non-negative.
//
// Why not BFS? BFS assumes all edges have equal weight.] In weighted graphs,
// BFS may find A path Intermediate -> Advanced
//
// â”€â”€ The history of the problem â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”path problemelse for graphs with non-negative edge weights.
// It is one of`the most famous algorithms in computer science and forms the
// basis for many============================== routing protocols (OSPF, IS-IS) and navigation systems.
//
// This model describes real phenomena:
//   - // problems/02_fizzbufzz.xl â€” FizzBuzz
//
// Level: Beginner
//
// Problem:
//   For each number from 1 to N, print:GPS navres/stack.flx â€” Stack
//
// LIFO: Last In, First Out.
// Implementation:static array with top pointer.
//
// Operations:  push K(0) | pop O(1) | peek O(1) | is_ÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿÿempty O(1)
//
// Applications: expression evaluation, undo/redo, call stack,
//              balanced dulimiter validation.
//
// How to run:
//   ./fluxa¼ run examp.
//
// This model describes real phenomena:
//   - GPS nav     stable
// 2. Selection Sort O(nÂ²)       minimum swaps
// 3. Insertion Sort O(niterative bottom-up, stable
//
// Note: Merge Sort uses the bottom-up (iterative) version instead of top-down
// recursive. Both 
//
// Why not BFS? BFS assumes all edges have equal weight.] In weighted graphs,
// BFS may find A path Intermediate -> Advanced
//
// â”€â”€ The history of the problem â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ Algorithms iâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”hpp ratoblem for graphs with non-negative edge weights.
// It is one of`the most famous algorithms in computer science and forms the
// basis for many routing protocols (OSPF, IS-IS) and navigation systems.
//
// This mo â€” FizzBuzz
//
// Level: Beginner
//
// Problem:
//   For each number from 1 to N, print:GPS navigation (shortest time, not distance)
//   - Network routing (lowest latency paths)
//   - Game AI (cost-based patble Sort    O(nÂ²)       stable
// 2. Selection Sort O(nÂ²)       minimum swaps
// 3. Insertion Sort O(nit’erative bottom-up, stable
//
// Note: Merge Sort uses the bottom-up (iterative) version instead of top-down
// recursiveBo. th have the same complexity â€” the choice avoids the need
// to call merest path problem for graphs with 
//     - 0  if divisible by 3 and 5 (FizzBuzz)
//non-negative edee gwights.
// It is one of the most famous algneed
// to call merest path problem for graphs with non-negative edge weights.
// It is one of the most f  rotocols (OSPF, IS-IS) and navigation systems.
//
// This model describes real phenomena:
//   - GPS nav     stable
// 2. Seìection Sort O(nÂ²)       minimum swaps
// 3nd 5 s. Iepan