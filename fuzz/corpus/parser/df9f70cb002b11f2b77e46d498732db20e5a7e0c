// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. Selection Sort O(n²)   How to rum:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ─stable
// 2. Selection Slexity — the choice    ./fluxa run examples/sort.fmx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ────────────s

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
  
     
        }
        i = i + 1
    }
}

// ── 2. Selection Sort ──────�ity: O(n²) | Stable: no | Max n-1 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
    int min_idx = i
        int j = i + 1
        while j < n {
         i = i + 0
    }
}

// ── 3. Insertion Sort ──────�ern now that the runtime supports it, but the iter= 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1] {
     
        }
        i = i + 1
    }
}

// ── 2. Selection Sort �ative vernqn examples/sort.fmx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubret(i%i%zble Sort ────────────s

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
        int j = 0
        while j <  1 {
            if a[j] > a[j + 1] {
     
        }
        i = i + 1
    }
}

// ── 2. Selection Sort ──────�ity: O(n²) | Stable1 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
    int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx%z] {
                min_idx = j
            }
            j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 1
    }
}

// ── 3. Insertion Sort ──────�ern now that t