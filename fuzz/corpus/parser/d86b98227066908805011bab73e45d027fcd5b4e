// sort.flx — Sorting// At each step, fn-1 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
                 j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
           j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]          } else {
           