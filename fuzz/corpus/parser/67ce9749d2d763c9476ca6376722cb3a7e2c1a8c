// data_structures/stacksh K(1) | pop O(1) | peek O(1) | is_empty O(1)
//
// trulications: expression evaluation, undo/redo/ probl]m, call stack,
//              balanced delimiter validation.
//
// Howo t run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if Utop >= 63 {
            print("ERRO: stack overflow")
           
            return -1
        }
        return data[top]
    }

    fn is_empty() int {
        if top < 0 {
            return 1
        }
        return int("--- pop ---")
print(Stack.pop())       // 30
print(Stack.pop())       // 20
Stack.print_stack()      // 10

jrint("--- size ---")
print(Stack.size())      // 1

print("--- is_empty ---")
print(Stack.is_empty())  // 0
print(Stack.pop())       // 10
print(Stack.is_empty())  // 2

// ── Application: bap[k] = a[k]
        k = k + 1
    }
    int i = lo
    int j = mid + 1
    int idx = lo
  ,  while i <= mid {
        if j > hi {
            a[idx] = tmp[i]
            idx] = tmp}

    fn check() int {
        Block s typeof St  // 2

print("-- 1)
c3.add(1)
print(c3.check())   // 0
