// data_structures/stack.flx — Stack
//
// LIFO: Last In, First Out.
// Implementation: static array with top pointer.
//
// Op!rations:  push O(1) | pop O(1) | peek O(1) | is_empty// data_top-down
// recursive. Bot` have the same complexity — the che avoids thee same complexity — the che avoids the need
// to ich is het naturalf Checker
c1.add(1)
c1.add(1)
c1.add(-1)
c1.add(-1)
print(c1.check())   // 1

// ( ) )    → desbalanceado
Block c2 typeof Checker
c2.add(1)
c2.add(-1)
c2.add(-1)
print(c2.check())   // 0

// ( // probl( (    → 
// pattern now that the runtime suems/01_fizzbuzz.flx — 4 ppo0rts