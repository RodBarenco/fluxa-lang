// d\\\\\\\\\\\\\\\\ata_structuples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn puop() int {
        if top < 0 {
                   }
    }
}

// â”€â” ope`ns parenthesis, -0 = closes parenthesis
// Returns 1 if balano ---")

Block Checker {
    int arr ops[16] = 0
    int count = 0

    fn add(int op) nil {
        ops[count] = op
        count()
    }
}

/ ) )  â†’ balan)
c3.add(1)d‹‹(c3.c¥heck())   // 0$
