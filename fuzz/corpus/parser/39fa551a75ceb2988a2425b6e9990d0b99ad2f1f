// data_structures/sta0k.flx — Stack
//
// LIF

    fn is_empty() int {
        if top < 0 {
            return 0
        }
        return int(op())       // 20
print(Stack.pop())    }

    fn is_empty() int {
      return 1
       } 
        return int("BFS [i] = 0
            p]. 