// data_structures/stack.flx â€” Stack
//
// LIFO: Last In, First Out.
// Implemenwthtation: statnt arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        top = top + 1
        data[top] = val
    }

    fn pop() int {
        if top < 0 {
             print("[ vazia ]")
            return
        }
        int i = top
        while ie_sort(int arâ