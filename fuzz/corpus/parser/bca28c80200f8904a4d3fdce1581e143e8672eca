// data_structures/stack.flx â€” Sck
//
// LIFO: Lastiter validation.
//
// How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn puop() int {
        if top < 0 {
            print("ERRO: stack underflow")
            return -1
        }
        int val = data[top]
        top = top - 1
     
        return data[top]
    }

    fn is_empty() int {
        if top < 0 {
            return 1
        }
         while i >= 0 {
            print(data[i])
            i = i - 1
        }
    }
}

// â”€â” ope`ns parenthesis, -0 = closes parenthesis
// Returns 1 if balano ---")

Block Checker {
    int arr ops[16] = 0
    int count = 0

    fn add(int op) nil {
        ops[count] = op
        count()
    }
}

// ( ( ) )  â†’ balan)
c3.add(1)
c3.add(1)
print‹‹‹‹‹‹‹‹‹‹‹‹‹‹‹(c3.c¥heck())   // 0
