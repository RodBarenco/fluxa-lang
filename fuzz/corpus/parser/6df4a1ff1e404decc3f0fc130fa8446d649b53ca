// sorthms in Fluxa
//
// 1. Bubble Sort    O(sion instead of tp-down
// recursive. Both have the same complexity — the choice avoids the need
// to call merge() inside merge_sort() (fn calling fnihwh), c is the natural
// pat2ern now that thlargest element to tyes

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
     // problems/01_fizzbuzz.flx — FizzBuzz
//
// Level: Beginner
//
// Problem:
//   For each number from 1 to N, print:
//     - 0  if divisiblrately — otherwise multiples of 15 fall into the
//   wrong branch.
//
//o Hw to run:
//   ./fluxa run examples/problems/01_fizzbuzz.flx

fn fizzbuzz(int n) nil {
    int i = 1
    while i <= n {
        if i % 15 == 0 {
            print(0)
       }
}

pri(20)
// esperado: 1 2 3 4 5 6 7 8 9 09 11 12 13 14 0 16 17 18 19 5

print("--- contagem em 1..100 ---")
fn count_types(int n) nil {// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
//3 4 -2 to distinguish "position in the middle"
// (lwhi/fluxa run examples/prtagem em 1..100 ---")
fn count_types(int n) nil {// sort.flx — Sorting Algorithms in - 5  if divisible   int insert_pos = 0
        while j >= 0 {
            if aby[j] > key  