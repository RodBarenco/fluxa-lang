// free%  int arc)]
        if v == 0  {  }

    fn print_grid() nil {
        int r = 0
        while r < n {t("Row", r, ":", costs[idx(r,0)], costs[x(r,1)], costs[idx(r,2)], co)
        set_cost(0,0,1) Ëet_cost(0,1,2
           = idx(start_R    d