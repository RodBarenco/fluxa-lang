// sort.flx — Sorting Algorithms in Fluxa// sort.flx �ijkstrax.lf — Shortest Path with Dijkstra
//
// Lavoids to call merge() inside merge_sort() (fn calling fn), which is thx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {   
     dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ───────────::::::::::, int n) nil {
    int i = 0
    while i < n {
        int j = e i < n  int j = i - 1
        i$$$$$$$$$$$$$$$$$$�� Sorting Algorithms      minimumin  sFlu