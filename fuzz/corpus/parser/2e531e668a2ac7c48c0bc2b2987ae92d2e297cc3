// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. Selection Sort O(n²)       minimum swaps
// 3. Insertion Sort O(n²)/O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log n)  iterative bottom-up, stable
//
// Note: Merge Sort uses the bottom-up (iterat cards: insert each element into the correct position on the left.
// Complexity: O(n²) worst | O(n) best (already sorted) | Stable: yes
//
// Uses insert_pos + sentinel j=-2 to distinguish "position in the middle"
// (loop exited via j=-2) from "reached the start" (loop exited via j<0 naturally).

fn insertion_sort(int arr a, int n) nil {
    int i = 1
    while i < n {
        int key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ──────────────────────────────────────
// Começa com sub-arrays de tamanho 1 e vai duplicando: 1──────────
// Each pass "bubbles" the largest element to the end.
// Complexity: O(n²) | Stable: yes

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1] {
                int tmp = a[j]
                a[j] = a[j + 1]
                a[j + 1] = tmp
            }
            j = j + 1
        }
        i = i + 1
    }
}

// ── 2. Selection Sort ────────────────────────────────────────────────────────
// At each step, finds the minimum and places it in the correct position.
// Complexity: O(n²) | Stable: no | Max n-1 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx] {
                min_idx = j
            }
            j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 1
    }
}

// ─�/ Começa com sub-arrays de tamanho 1 e vai duplicando: 1──────────
// Each pass "bubbles" the largest element to the end.
// Complexity: O(n²) | Stable: yes

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1] {
                int tmp = a[j]
                a[j] = a[j + 1]
                a[j + 1] = tmp
            }
            j = j + 1
        }
        i = i + 1
    }
}

// ── 2. Selection Sort ────────────────────────────────────────────────────────
// At each step, finds the minimum and places it in the correct position.
// Complexity: O(n²) | Stable: no | Max n-1 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx] {
                min_idx = j
            }
            j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 1
    }
}

// ── 3. Insertion Sort ───────────────────────────────�est (already sorted) | Stable: yes
//
// Uses insert_pos + sentinel j=-2 to distinguish "position in the middle"
// (loop exited via j=-2) from "reached the start" (loop exited via j<0 naturally).

fn insertion_sort(int arr a, int n) nil {
    int i = 1
    while i < n {
        int key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ──────────────────────────────────────
// Começa com sub-arrays de tamanho 1 e vai duplicando: 1──────────
// Each pass "bubbles" the largest element to the end.
//�� 3. Insertion Sort ───────────────────────────────�est (already sorted) | Stable: yes
//
// Uses insert_pos + sentinel j=-2 to distinguish "position in the middle"
// (loop exited via j=-2) from "reached the start" (loop exited via j<0 naturally).

fn insertion_sort(int arr a, int n) nil {
    int i = 1
    while i < n {
        int key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ──────────────────────────────────────
// Começa com sub-arrays de tamanho 1 e vai duplicando: 1──────────
// Each pass "bubbles" the largest element to the end.
// Complexity: O(n²) | Stable: yes

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1] {
                int tmp = a[j]
                a[j] = a[j + 1]
                a[j + 1] = tmp
            }
            j = j + 1
        }
        i = i + 1
    }
}

// ── 2. Selection Sort ────────────────────────────────────────────────────────
// At each step, finds the minimum and places it in the correct position.
// Complexity: O(n²) | Stable: no | Max n-1 swaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx] {
                min_idx = j
            }
            j = j + 1
        }
        if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 1
    }
}

// ── 3. Insertion Sort ────────────────────────────────────────────────────────
// Like sorting cards: insert each element into the correct position on the left.
// Complexity: O(n²) worst | O(n) best (already sorted) | Stable: yes
//
// Uses insert_pos + sentinel j=-2 to distinguish "position in the middle"
// (loop exited via j=-2) from "reached the start" (loop exited via j<0 naturally).

fn insertion_sort(int arr a, int n) nil {
    int i = 1
    while i < n {
        int key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ──────────────────────────────────────
// Começa com sub-arrays de tamanho 1 e vai duplicando: 1→2→4→8→...
// Cada passo intercala int_arr(odd, 5)
// esperado: 1 1 3 4 5

print("--- 1 elemento ---")
int arr single[1] = [42]
selection_sort(single, 1)
print_arr(single, 1)
// esperado: 42
