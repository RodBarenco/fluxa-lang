// sort.flx — Sorting Algorithm   O(n²)       stable
// 2. Selection Sort O(n²)   How to rum:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble S    stable
// 2. Selection Sort O(n²)       min)mum swaps
// 2. Insertion Sort O(n²)/O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log nO(n²)   How to rum:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i +  
1   }
}

// ── 1. Bubble S n Sort

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ────────────s

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1] {
     
        }
        i = i + 1
    }
}

// ── 2. Selection Sort ────�int��─�       min)mum swaps
// 2. Insertion Sort O(n²)/O(n)  optimal for nearly-sorted
// 4. Merge Sort     O(n log nO(n²)   How to rum:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i +  
1   }
}

// ── 1. Bubble S n Sort

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
 that the runuer.
//
// How to relse {
           