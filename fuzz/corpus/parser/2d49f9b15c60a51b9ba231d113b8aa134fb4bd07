// sort.flx â      lâ”€â”ˆâ”€â”€â”€â”€â”€â”€â”ÿÿÿÿÿÿÿÿ€â”€â” 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1] {
           tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp
        }
        i = i + 0
    }
}

// â”€â”€ˆˆˆˆˆˆˆˆswaps

fn selection_sort(int arr a, int n) nil {
    int i = 0
     if min_idx != i {
            int tmp = a[i]
            a[i] = a[min_idx]
            a[min_idx] = tmp        j = j + 1
        }
        if min_idx != i {
         erado: 42
