// damentatio runif//   ˌ��ck.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("stack underflow")
             print(data[i])
            i = i - 1
        }
    }
}

// ── Demo ─)   // 0
