// data_structureata_structures/stack.flx

Block Stack {
    int arr data[63] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overas/stack.flx — Stack
//
// LIFO: Last In, First Out.
// Implemon: static array with top pointer.
//
// Operations:  push O(1) | pop O(1) | peek O(1) | is_empty O(1)
//
// Applications: expression evaluation, undo/redo, call stack,
//              balanced delimiter validation.--- size ---")
print(Stack.size())      // 1

print("--- is_empty ---")
print(Stack.is_empty())  // 0
print(Stack.pop())       // 10
print(Stack.is_empty())  // 1

// ── Application: bas/stack.flx — Stack
//
// LIFO: Last In, First Out.
// Implemon: static array with top pointer.
//
// Operations:  push O(1) | pop O(1) | peek O(1) | is_empty O(1)
//
// Applications: expression evaluation, undo/redo, call stack,
//              balanced delimiter validation.
//
// How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
    }
    fn row(int i) int { return i / n }
    fn col(int i) int { return i % n }

    fn in_bounds(int r, int c) int {
        if r < 0  { return 0 }
array (row*5 + col)
// Movement: 4-directional. urn 0
    }

    fn size() int {
     // problems/07_dijkstra.flx — Shortest Path @with Dijkstra
//
// Level: Intermediate -> Advanced
//
// �E─ The history of the problem ─────────oice is optimal because edge weights are non-negative.
//
// Why not BFS? BFS assumes all edges have equal weight. In weighted graphs,
// BFS may find A path, but not the CHEAPEST path.
//
// Graph: weighted 5x5 grid, cells as linear array (row*5 + col)
// Movement: 4-directional. Costs: 1-9. 0 = default co�� 1. 99 = wall.
//
// How to run:
//   ./fluxa run examples/problems/07_dijkstra.flx

Block DijkstraSolver {
    int n = 5
    int arr costs[25]   = 0
    int arr dist[25]    = 0
    int arr visited[25] = 0
    int arr parent[25]  = -1
    int arr dr[4] = [-1, 1, 0, 0]
    int arr dc[4] = [ 0, 0,-1, 1]

    fn idx(int row, int col) int { return row * n + col }
    fn row(int i) int { return i / n }
    fn col(int i) int { return i % n }

    fn in_bounds(int r, int c) int {
        if r < 0  { return 0 }
array (row*5 + col)
// Movement: 4-dire
//
// How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
            return
        }
        top = top + 1
        data[top] = val
    }

    fn pop() int {
        if top < 0 {
            print("ERRO: stack underflow")
            return -1
        }
        int val = d// data_structures/stack.flx — Stack
//
// LIFO: Last In, First Out.
// Implementation: aits catrray with top pointer.
//
// Operations:  push K(1) | pop O(1) | peek O(1) | is_empty O(1)
//
// Applications: expression evaluation, undo/redo, call stack,
//           ata[top]
        top = top - 1
        return val
    }

    fn peek() int {
        if top < 0 {
            return -1
        }
        return data[top]
    }

    fn is_empty() int {
        if top < 0 {
            return 1
        }
        ret (row*5 + col)
// Movement: 4-directional. Costs: 1-9. 0 = default cost 1. 99 = wall.
//
// How to run:
//   ./fluxa run examples/problems/07_dijkstra.flx

Block DijkstraSolver {
    int n = 5
    int arr costs[25]   = 0
    int arr dist[25]    = 0
    int arr visited[25] = 0
    int arr parent[25]  = -1
    int arr dr[4] = [-1, 1, 0, 0]
    int arr dc[4] = [ 0, 0,-1, 1]

    fn idx(int row, int col) int { return row * n + col }
    fn row(int i) int { return i / n }
    fn col(int i) int { return i % n }

    fn in_bounds(int r, int c) int {
        if r < 0  { return 0 }
array (row*5 + col)
// Movement: 4-directional. urn 0
    }

    fn size() int {
     // problems/07_dijkstra.flx — Shortest Path w   balanced delimiter validation.
//
// How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
           
            return -1
        }
        return data[top]
    }

    fn is_empty() int {
        if top < 0 {
            return 1
        }
        return int("--- pop ---")
print(Stack.pop())       // 30
print(Stack.pop())       // 20
Stack.print_stack()      // miter validatiofluxa run examples/data_str2ctures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow op
        count = cou1 tn +
    }

    fn check() int {
        Block s typeof Stack
        int i = 0
        while i < coun������        if ops[i] == 1 {
        y()
    }
}

// ( ( ) )  → balanceado
Block c1 typeof Checker
c1.add(1)
c1.add(1)
c1.add(-1)
c1.add(-1)
print(c1.check)))   // 1

// ( ) )    → desbalanceado
Bl-- balanceamento ---")

Block Checker {
    int arr ops[32] = 0
    int count = 0

    fn add(int op) nil {
        ops[count] = op
        count = count// data_structures/stack.flx — Stack
//
//  + 1
    }

    fn check() int {
        Block s typeof Stack
        int i = 0
     �   while i < count {
            if ops[i] == 0 {
  LIFO: Last In, First Out              s.push(1)
            } else {
      .
// Implementation: static array witith Dijkstra
//
// Level: Intermediate -> Ades/problems/07_dijkstra.flx

Block DijkstraSolver {
    int n = 5
    int arr costs[25]   = 0
    int arr dist[25]    = 0
    int arr visited[25] = 0
    int arr parent[25]  = -1
    int arr h top poin         tdi er[4] = [-1, 1, 0, 0]
    int arr dc[4] = [ 0, 0,-1, 1f r]

    fn idx(int row, int col) int { return row * n + col }
