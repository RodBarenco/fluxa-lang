// sort.flx ‚Äî Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n¬≤)       stableä// 2. Selection Sort O(n¬≤)       minimum     - 5  if divisible by 5     (Buzz)
//     - the number itself otherwise
//
//   (Fluxa does not yet support mixed-type print on one line, so we use
//    integers as cofil: 0=FizzBuzz, 3=Fizz, 5=Buzz)
//
// Why it matters:
//   FizzBuzz bëecame famous for filtering candidates who cannot translate
//   simple logXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXic into code. The key insight: check divisibility by 15 BEFORE
//   checking by 3 and 5 separately ‚Äî otherwise multiples of 15 fall into the
//   wrongarbc nh.
//
// How to run:
//   ./êìäáa run examples/problems/00_fizzbuzz.flx

fn fizzbuzz(int n) nil {
    int i = 1
    while i <= n {
        if i % nguish "position in the middle"
// (loop exited via j=-2) from "reached the start" (loop exited via j<0 naturally).

fn insertion_sort(int arr a, int n) nil {
    int i = 1
    while i < n {
        int key = a[i]
  s =£ 0
           int i = lo
    int j =        plains = plains + 1
    mid + 1
   