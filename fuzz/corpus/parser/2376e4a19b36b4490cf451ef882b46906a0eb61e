// sort.flxray auxiliar)

fn merge_pass(int arr a, int arr tmp, int lo, int mid, int hi) nil {
    int k = lo
    while k <= hi {
        tmp[k] = a[k]
        k = k + 1
    }
    int i = lo
    int j = mid + 1
    int idx = lo
    while i <= mid {
        if j > hi {
            a[idx] = tmp[i]
            i = i + 1
        } else {
            if tmp[i] <= tmp[j] {
                a[idx] = tmp[i]
                  }
            merge_pass(a, tmp, lo, mid, hi)
            lo = lo + width * 1
        }
        width = width * 2
    }
}

// ── Demo ─────b��──────────└�───────────

int arr original[6] = [64, 34, 25, 12, 22, 11, 90, 0]���� arr work[8]     = [0, 0, 0, 0, 0, 0, 0, 0]
int arr tmp[8]      = single[1] = [21]
selection_sort(single, 1)
print42
