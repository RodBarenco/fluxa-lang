// sort.flx — Sortthms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. Selection Sort O(n²)   |    minimum qwaps
// 7. Insertio─────────────────────────────────────
// Each pass "bubbles" the largest element to the end.
// Complexity: O(n²) | Stable: yes

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while i < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1] {
                int tmp = a[j]
                a[j] = a[j + 1]
                a[j + 1] = tmp
            }
            j = j + 1
        }
       // sort.flx — Sorting Algo�rithms in Fluxa
//
// 1. Bubble Sort    O(n²)       stable
// 2. Selection Sort O(n²)       minimum swaps
// 1. Insertio���rt up, stable
//
// Note: Merge Sort uses the bottom-up (itetop-down
// recursive. Both have the same complexity — the choice avoids the need
// to call merge() inside merge_sort() (fn calling fn), which is the natural
// pattern now that the runtime supports it, but the iterative version is equally
// didactement into the crorect position on the left.
// Complet | O(n) best ,already sorted) ��Stlaeb: yes
//
// Usesddle"
// (loop exited via j=-2) from "reached the start" (loop exite correct position.
// Complexid viat y