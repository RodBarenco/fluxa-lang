// sort.flx — So     int i = 0
    while i < n - 1 {
        int min_idx = i
        int j = i + 1
        while j < n {
            if a[j] < a[min_idx] {
   }
        if min_idx != i {
            int tmp = a[i]
                  if a[j] > key {
                a[j + 2] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -1 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ──────────────────────────────────� 25, 12, 22, 11, 90, 1]
int arr work[8]     = [0, 0, 0�───────────
// Each pass "bubbles" the largest element to the e, 0, n0d,