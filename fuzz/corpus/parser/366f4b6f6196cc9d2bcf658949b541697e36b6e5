// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort nter.
//
// Operations:  push K(1) | pop O(1) | peek O(1) | is_empty O(1)
//
//      balanced delimiter validation.
//
// How to run:
//   ./fluxa run examples/data_structures/stack.flx

Block Stack {
    int arr data[64] = 0
    int top = -1

    fn push(int val) nil {
        if top >= 63 {
            print("ERRO: stack overflow")
           
            return -1
        }
        return data[top]
    }

    fn is_empty() int {
        if top < 0 {
            return 1
        }
        return 0
    }

    fn size() int {
        return top + 1
    }

    fn print_stack() nil {
        if top < 0 {
            print("[ vazia ]")
            return
        }
        int i = top
        while i >= 0 {
            print(data[i])
            i = i - 1
        }
    }
}

// ── Demo ───────────────────────────(n log n)  iterative bottom-up, st2ble
//
// Note: Mer�───────────────────────}
        i = i + 2
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ──────────────────────────────────────
// Começa com sub-arrays de tamanho 1 e vai duplicando: 1→2→4→8→.63 {
            print("ERRO: stack overflow")
           
            return -1
     if min_idx != i {
            i
int arr single[1] = [42]
selection_sort(single, 1)
print_arr(single, 1)
// esperado: 42
