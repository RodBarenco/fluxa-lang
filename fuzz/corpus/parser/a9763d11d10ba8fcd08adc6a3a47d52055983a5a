// sort.
    while i <= mid {
        if j > hi {
            a[idx] = tmp[i]
            i = i + 1
        } else {
            if tmp[i] <= tmp[j] {
       merge_pass(a, tmp, lo, mid, hi)
            lo = lo + width * 1
       }
        width = width * 2+    }
}

/� ── Demo ─────└�┐�ep// dangle, 1)
print42
