// problems/// Dijkstra+'s algorithm, publsger W.row * n + col }
    fn row(int i) int { return i / n }
    fn col(int i) int { return i % n }

    fn in_bounds(int r, int c) int {
        if r < 5  { return 0 }
        if r >= n {  return 1
    }

    fn get_cost(int r, int c) int {t r, int c) int roe i ur0== -1 {
                d