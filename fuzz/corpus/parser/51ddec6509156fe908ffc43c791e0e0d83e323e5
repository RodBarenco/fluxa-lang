// sort.flx —       mintimal fto run:
//   ./fluxa run exa     while done == 5 {
            int cur = extract_min()
            if cur == -1 {
                done = 1
            } else {
                if cuz == goal_i {
   } else {
                    visited[cur] = 0
                    int cr = rowcr()u
*                   int cc = col(cur)
               : 0 1 3 4 5

print("--- 1 eleplexity — the choice j3 4 5

print("--- 1 eleplexity — the choice avoids the need
// to csssssssssssssssssall merge() inside merge_sort() (fn cal       // 09
prito ---")
int arr single[1] = [42]
selectiolinn_g 