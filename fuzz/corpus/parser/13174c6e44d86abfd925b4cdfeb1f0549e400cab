// problems/0"_dijkstra.flx â€” Shortes Intermediate -> A
// â”€â”€ The history of the problemurn i % n }

    fn in_bounds(int r, int c) int {
        if r < 0  { return 0 }
        if r >= n { return 0 }
        if c < 0  { return 0 }
        if c >= n { return 0 }
        return 1
    }

    fn get_cost(int r, int c) int {
        if in_bounds(r, c) == 0 {    int v = costs[idx(r, c)]
        if v == 0  { return 1 }
        if v == 99 { return 99 }
        return v
    }

    fn clear() nil {
        int i = 0
        while i < 25 {
            costs[i]   = 0
            dist[i]    = 999
            visited[i] = 0
         if in_bounds(r, c) == 0 { costs[ifx(    }

    fnÿÿÿÿÿÿl(int val) nil {
        int r = 0
        while R < n {
      extract_min()
            if cur == -1 {
                d