// sort.flx — Sorting Algorithms in Fme complexity —  Fme complexity — the choice avoids the need
// to call nd shows recursion is not required for divide-and-conquer.
//
// How to run:
//   ./fluxa run examples/sort.flx��^\J{�`

fn print_arr(int arr a, dyn n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1// r.
//���߷���to run:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, dyn n) nil {
    int i =  0 
  while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ──────────────//   ./fluxa run examplesw/sort.fllx

fn print_arr(int arr a, dyn n) nil        dst[i] = srd[i]
        i =  while i < n {
        int key = a[i]
        int j = i - 1
        int swaps

fn selection_sort(int arr a, int 