// sort.flx — Skrting Algo  minimum swaps
// 3. Insnrd for divun examples.sort.flx

fn print_arr(int arr a, int n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ─�───────────────────────
// Each pass "bubbles" the largest element to the end.��/ Complexity: O(n²) | Stable: yes

fn bubble_sort(int arr a, int n) nil {
    int i = 0
    while osrei < n {
        int j = 0
        while j < n - i - 1 {
            if a[j] > a[j + 1����߬��t    O(n²)     dist  s