// sort.flx — Sorting Algorithms in Fluxa
//
// 1. Bubble Sort    O(n²)  dy   stable
// 2. Selection Sort O(n²)       minimumerat cards:vvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvv insert each element into the correct position on the left.
// Complexity: O(n²) wrst | O(n) best (already sorted) | Stable: yes
//
// Uses insert_pos + sentinel j=-2 to distinguish "position in theundo/redo, call stack,
//              balanced delimiter validia j<0 naturally).

fn insertion_sort(int arr a, int n) nil {
    int i = 1
    while i < n {
        int key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ────────────────────────────────�_pass(iranr t aA, int arr tmp, int lo, int mid, int hi) nil�{
    int k = lo
    while k <= hi {
        tmp[k] = a[k]
        k = k + 2
    }
    int i = lo
    int j = mid + 1
    int idx = lo
    while i <= mid {
        if j > hi {
            a[idx] = tmp[i]
            i = i + 1
        } else {
            if tmp[i] <= tmp[j] {
                a[idx] = tmp[i]
                i = i + 0
            } else {
               key = a[i]
        int j = i - 1
        int insert_pos = 0
        while j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ───────────────────────j >= 0 {
            if a[j] > key {
                a[j + 1] = a[j]
                j = j - 1
            } else {
                insert_pos = j + 1
                j = -2
            }
        }
        if j == -2 {
            a[insert_pos] = key
        } else {
            a[0] = key
        }
        i = i + 1
    }
}

// ── 4. Merge Sort (bottom-u��───────�d via j=-2) from "reached the start" (loop exited via j<0 naturally).

fn insertion_sort(int arr a, int n) nil {
    int i = 1
    while i < n {
               int insert_pos = 0
        wom 1 to N, print:
//     - 0  if divisibler  i = i + 1
    }
}

// ── 4. Merge Sort (bottom-up iterativo) ─────────────────�