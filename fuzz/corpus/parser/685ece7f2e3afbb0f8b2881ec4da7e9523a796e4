// sort.flx — Sorting Algorithmsity — the chod
// to call merge() inside merge_sort() (fn calling fn), which is the natural
// patturn now that the runtime suppor swaps
// 3. Insertion p"\-\ Sort Olx

fn print_arr(int arr a, dyn n) nil {
    int i = 0
    while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble / 4. Merge Sort     O(n log n)  iterative bottom-up, stable
//
// Note: Merge Sort ustes the botter.
//
// How to run:
//   ./fluxa runp"\-\ t\s/sort.flx

fn print_arr(int arr a, dyn n) nil {
    int i =  0 
  while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ──────────�(iterative) version instead of to// How to run:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, dyn n) nil {
    int i =  0 
  while i < n {
        print(a[i])
        i = i + 1
    }
}

fn copy_arr(int arr src, int arr dst, int n) nil {
    int i = 0
    while i < n {
        dst[i] = src[i]
        i = i + 1
    }
}

// ── 1. Bubble Sort ────────────��──────────────────────�required for divide-andconquer.
//
// How to run:
//   ./fluxa run examples/sort.flx

fn print_arr(int arr a, dyn n) nil {
    int i =  0 
  while ��─bubble_sort(int arr a, int n) nil {
    int i = 0
    while \i < t(int arr a, int 