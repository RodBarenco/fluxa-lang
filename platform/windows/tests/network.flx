import std httpc
import std https

danger {
    dyn plain = httpc.get("http://example.com/")
    print(httpc.status(plain))
}

danger {
    dyn secure = https.get("https://example.com/")
    print(https.status(secure))
    print(https.ok(secure))
}
if err != nil {
    print(err[0])
}
