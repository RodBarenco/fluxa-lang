# Windows platform sources

The complete Windows build, runtime, testing, standalone distribution,
Fluxa Builder integration, VirtualBox, and Mesa3D guide is:

**[Fluxa Lang Windows runtime and distribution](../../docs/WINDOWS.md)**

The files in this directory implement the native Windows entrypoint,
compatibility layer, static Raylib build, and Windows-specific tests.
